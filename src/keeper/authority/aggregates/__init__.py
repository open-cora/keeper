"""Aggregate kernels for the Authority bounded context.

The same shape and the same caveat as the Access side: a sibling reaches
under `aggregates` and never into `features`, and how wide that door
should be is settled by a tach interface declared once a real sibling
exists, not guessed before one does.
"""

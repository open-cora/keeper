"""Infrastructure layer: ports (Protocols) and adapter implementations.

The domain and application layers depend on Protocols defined here; only adapters
import third-party libraries (asyncpg, MCP SDK, etc.).
"""
